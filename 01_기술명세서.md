# 기술명세서 — 해외결제 분쟁 첫 72시간 에이전트 (가칭 "분쟁72")

작성일 2026-09-04 · 버전 0.1 · 대상 대회: 2026 금융 AI Challenge

---

## 1. 서비스 한 줄 정의

해외 사업자(AI API, 구독 서비스 등)로부터 받은 청구 메일과 카드 승인·거절 문자를 붙여넣으면, AI 에이전트가 **진위 → 결제 상태 → 적용 규정 → 이의신청 기한 → 제기 경로 → 제출 서류**를 한 번에 판정·생성하는 웹서비스. 서류를 만들기만 하고 발송·접수는 사용자가 직접 한다.

## 2. 해결하는 문제 (요약)

- 해외 사업자는 AI 봇으로만 응대한다. 251억 원 오청구 사례에서 피해자는 메일 15통을 보내고 4일 뒤에야 자동 회신을 받았다.
- 국내 카드사 해외 이의신청은 브랜드사(Visa·Mastercard) 사유코드에 맞는 증빙을 90~120일 내 갖춰야 하고, 처리에 최대 5개월이 걸린다. 금감원은 2026년 6월 이런 분쟁으로 신용카드 민원이 88% 급증했다고 발표했다.
- 피해자는 "사업자 절차"와 "카드사 절차"를 동시에 모른다. 그래서 카드 전체 정지라는 최후 수단부터 쓴다.

## 3. 사용자와 사용 시점

| 사용자 | 상황 | 사용 시점 |
|---|---|---|
| 개인 개발자·소규모 팀 | API 키 유출·과금 오류로 거액 청구 | 청구 메일 수신 직후 |
| 일반 이용자 | 해지한 해외 구독이 계속 결제 | 카드 문자 확인 직후 |
| 해외직구 이용자 | 미배송·이중 청구 | 사업자 무응답 확인 후 |

## 4. 화면 명세

화면은 두 개다. 기능이 UI만 보고 읽혀야 하므로 장식·탭·사이드바를 두지 않는다.

### 4.1 입력 화면 (S-01)
- 제목 한 줄: "해외결제 청구 메일과 카드 문자를 붙여넣으세요."
- 부제 한 줄: 무엇을 판정하고 무엇을 만드는지, 그리고 "보내는 것은 직접 합니다"를 명시.
- 입력창 하나(textarea). 플레이스홀더에 붙여넣기 순서(메일 원문 → 카드 문자 → 내 상황) 안내.
- 버튼 하나: "판정 시작".
- 예시 3개(칩 버튼): 클릭하면 합성 데이터가 입력창에 채워진다.
- 이미지 업로드는 v0.1 범위 밖. 텍스트 붙여넣기만.

### 4.2 결과 화면 (S-02)
입력 화면과 같은 페이지에서 전환. 위에서 아래로 단일 컬럼, 최대 폭 760px.

1. **판정 과정** — 6단계 로그. 각 단계는 이름 + 한 줄 근거를 실시간으로 표시. (예: "발신 도메인 mail.anthropic.com → Anthropic 정식 도메인과 일치")
2. **결론** — 결론 한 문장(22px), 결제 상태 한 문장, 이의신청 기한(D-숫자, 40px, 30일 이하면 경고색), 지금 할 일 최대 3개(긴급도: 지금/오늘/이번 주).
3. **어디에 제기할 수 있나** — 세 경로(사업자 이의제기 / 카드사 이의신청 / 소비자원 상담)를 우선순위 번호와 함께 표시. 해당 없는 경로는 흐리게 + "해당 없음" + 이유. 해당 경로에는 "갖고 있는 증빙 / 더 필요한 증빙" 두 목록.
4. **보낼 서류** — 탭 3개: 영문 메일, 국문 진술서, 타임라인. 각 서류에 복사 버튼. 발송 버튼은 없다.
5. **판정에 쓴 규정** — 검색된 규정 원문 요약과 출처. "원문 확인 필요" 표시가 있는 규정은 그대로 노출.
6. 하단 고지: 법률 자문이 아님, 접수 전 카드사 확인, 승인 시도가 반복되면 카드사에 전화 우선.

## 5. 기능 명세

| ID | 기능 | 입력 | 처리 | 출력 | 구현 방식 |
|---|---|---|---|---|---|
| F-01 | 청구 내용 읽기 | 사용자 붙여넣기 텍스트 | 가맹점·금액·통화·일자·승인/거절/청구서만 상태·발신 도메인·인보이스 번호·사건 유형·보유 증빙 추출 | 구조화 JSON | LLM (구조화 추출) |
| F-02 | 발신자 진위 확인 | F-01의 발신 도메인 | 알려진 사업자·결제대행사 도메인 목록과 대조 | official / suspicious / no_sender + 근거 | 코드 (규칙) |
| F-03 | 적용 규정 찾기 | 사건 유형 + 원문 | 지식베이스에서 사유코드·카드사 서류 요건·소비자원 절차 검색 | 규정 목록(최대 6건) | 코드 (키워드·유형 점수) |
| F-04 | 기한 계산 | 승인 거래 여부 + 첫 승인일 | 승인 거래 있으면 90일(안전)/120일(최대) 잔여일 계산. 없으면 "대상 아님" | D-값 또는 null | 코드 |
| F-05 | 경로 판정 | F-01~F-04 전체 | 세 경로의 적용 가능 여부·우선순위·사유코드·증빙 갭·즉시 행동 판단 | 판정 JSON | LLM (규정 근거 추론) |
| F-06 | 서류 작성 | F-01, F-05, F-03 | 영문 이의제기 메일, 국문 진술서, 타임라인 생성 | 서류 JSON | LLM |
| F-07 | 실시간 진행 표시 | F-01~F-06 이벤트 | 각 단계 시작·완료·근거를 스트리밍 | UI 로그 | SSE |
| F-08 | 예시 데이터 | 칩 클릭 | 합성 시나리오 3종 주입 | 입력창 채움 | 정적 데이터 |

### 5.1 판정 원칙 (F-05 프롬프트에 고정)
- 승인 완료 거래가 없으면 카드사 경로는 "해당 없음". 인보이스만 발행된 상태, 승인 거절 상태가 여기 해당.
- 본인 계정에서 발생한 사용량은 부정사용 코드(Visa 10.4, MC 4837)가 아니라 서비스 분쟁 코드(Visa 13.x, MC 4853)로 분류.
- 가맹점에 먼저 연락한 기록이 없으면 사업자 경로가 카드사 경로보다 선행.
- 카드 전체 정지보다 해외결제 차단(안심설정)을 먼저 권한다. 단, 승인 시도가 반복되고 승인될 위험이 있으면 정지도 정당.
- 규정에 없는 절차는 만들어내지 않는다.

### 5.2 사건 유형
billing_error(사용량 없는데 청구) · credential_theft(키·계정 탈취 사용량) · cancelled_recurring(해지 후 계속 결제) · not_received(미제공) · duplicate(이중 청구) · unknown

## 6. 에이전트 파이프라인

```
입력 텍스트
  → [F-01 파서: LLM]         → parsed
  → [F-02 진위: 코드]        → verify
  → [F-03 규정 검색: 코드]   → rules[]
  → [F-04 기한: 코드]        → deadline | null
  → [F-05 판정: LLM]         → judgment   (입력: parsed, verify, deadline, rules, 원문)
  → [F-06 서류: LLM]         → drafts     (입력: parsed, judgment, rules)
각 단계 완료 시 SSE 이벤트 {step, status, note, data} 전송
```

LLM 호출은 3회로 고정(파서·판정·서류). 도구 호출(tool use) 방식 대신 순차 파이프라인으로 구현한다. 이유: 3일 안에 디버깅 가능해야 하고, 각 단계의 근거를 화면에 그대로 보여줘야 하므로 순서가 고정된 편이 낫다. "에이전트"라는 명칭은 사용자가 목표만 주면 여러 단계를 자율 수행한다는 의미로 쓰고, 되돌릴 수 없는 행동(발송·접수)은 하지 않는다.

## 7. 데이터

### 7.1 지식베이스 (`lib/rules.ts`, 정적)
- Visa 사유코드: 10.4, 12.5, 13.1, 13.2, 13.6
- Mastercard 사유코드: 4837, 4853, 4834
- 국내 카드사 절차: 이의신청 기한(90~120일), 승인 완료 거래만 대상, 부정사용 신고 시 해외거래 정지 선행, 사유별 필수 서류
- 소비자원 국제거래 소비자포털: 가맹점 45일 답변 의무, 120일 신청 기한, 상담 경로
- 금감원 안내: 판정 권한은 브랜드사, 최대 5개월
- 해외 사업자 에스컬레이션 요령
각 항목: id, source, title, body, caseTypes[], keywords[], verify(원문 확인 필요 여부)

### 7.2 정식 발신 도메인 목록 (`KNOWN_SENDERS`)
Anthropic, OpenAI, Google, X, AWS, Stripe. 확장 가능.

### 7.3 합성 데모 데이터 (`lib/samples.ts`)
1. 사용량 0인데 25억 원 청구서 — 승인 거절 반복, 인보이스만 존재 → 카드사 경로 해당 없음, 사업자 경로 최우선
2. 유출된 API 키로 48시간에 8만 달러 — 승인 완료 2건, 법인카드 → 카드사 경로 가능하나 부정사용 아닌 서비스 분쟁, 사업자 경로 선행
3. 해지 요청 후에도 결제된 구독 — 해지 캡처 보유 → Visa 13.2 / MC 4853, 카드사 경로 즉시 가능

실제 금융 데이터·개인정보는 쓰지 않는다. 사례는 보도 구조만 참고한 가상 데이터.

## 8. API 명세

### POST `/api/agent`
- 요청: `{ "input": string }` (최소 20자)
- 응답: `text/event-stream`. 각 이벤트는 `data: {JSON}\n\n`
  - `{step: "parse"|"verify"|"rules"|"deadline"|"judge"|"draft", status: "running"|"done"|"error", note?: string, data?: object}`
  - 마지막: `{step: "final", status: "done", data: {parsed, verify, rules, deadline, judgment, drafts}}`
  - 실패: `{step: "fatal", status: "error", note: string}`
- 오류: 400(입력 부족), 500(API 키 미설정)

### 데이터 스키마

**parsed**
```
{ case_type, merchant, sender_domain|null, invoice_id|null,
  amounts: [{amount, currency, date|null, status: approved|declined|invoice_only|unknown}],
  card_issuer|null, card_blocked|null, user_summary,
  first_transaction_date|null, merchant_contacted: bool, evidence_present: string[] }
```
**judgment**
```
{ headline, payment_state,
  immediate_actions: [{action, why, urgency: now|today|this_week}] (≤3),
  routes: [{name: merchant|issuer|kca, label, applicable, priority, reason,
            reason_code|null, evidence_have[], evidence_missing[]}] (항상 3개) }
```
**drafts**
```
{ merchant_email_en, issuer_statement_ko,
  timeline: [{date, event, source: user|merchant|issuer|agent}] }
```

## 9. 기술 스택

- Next.js 14 (App Router), TypeScript, React 18
- 스타일: 전역 CSS 하나. Tailwind·UI 라이브러리 사용 안 함(빌드 변수 줄이기)
- LLM: Anthropic Messages API, `@anthropic-ai/sdk`. 모델은 환경변수 `ANTHROPIC_MODEL`로 지정
- 폰트: Pretendard (CDN)
- 배포: Vercel (서버리스 함수 타임아웃 120초 설정). API 키는 서버 환경변수에만 존재, 클라이언트 노출 없음
- 저장소: 없음. 세션 상태는 브라우저 메모리. 개인정보 저장 안 함

## 10. 비기능 요건

- 응답: 입력 후 첫 단계 완료까지 5초 이내, 전체 60초 이내
- 보안: API 키 서버 전용, 입력 텍스트 로그 저장 금지, 외부 링크 자동 열기 없음
- 가용성: 제출 후 5일 이상 URL 유지. 무료 호스팅 한도 내에서 하루 100회 판정 가정
- 접근성: 키보드 포커스 표시, reduced-motion 존중, 모바일 폭(380px)에서 단일 컬럼 유지
- 실패 처리: LLM JSON 파싱 실패 시 해당 단계 error 표시 + 이전 단계 결과는 유지

## 11. 범위 밖 (v0.1에서 하지 않는 것)
- 카드사 이의신청 실제 접수(API 연동)
- 사업자 메일 자동 발송
- 이미지 OCR, 이메일 계정 연동
- 사용자 계정·이력 저장
- 구독 결제 사전 알림(기존 서비스 영역)

## 12. 다음 단계 (기획서 로드맵용)
1. 카드사 앱 "해외 이의신청" 접수 폼과 연동해 진술서·증빙을 바로 제출
2. 카드사 FDS(이상거래탐지)에서 해외 고액 승인 시도 반복 감지 시 본 서비스로 자동 안내
3. 이메일 전달 주소 제공: 청구 메일을 전달만 하면 판정 시작

## 13. 데모 시나리오 (발표 5분)
1. 실제 사례 타임라인 한 장: 7/7 청구 → 15통 메일 → 7/11 자동 회신 → 카드 정지
2. 예시 1 클릭 → 판정 시작 → 6단계 로그가 흐르며 근거가 보임
3. 결론: "카드사 이의신청 대상 아님 — 승인된 결제가 없음", 지금 할 일: 해외결제만 차단 / 사업자 단일 티켓 / 콘솔 캡처 확보
4. 영문 메일 탭 → 복사
5. 예시 3 클릭 → D-값이 뜨고 카드사 경로가 1순위로 바뀌는 대비
